[project]
name = "loggingdecorators"
version = "0.1.3"
description = "NONE"
authors = [
    { name = "Adam Tuft", email = "adam.s.tuft@gmail.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "pytest>=7.4.3",
]

[tool.hatch.metadata]
allow-direct-references = true
